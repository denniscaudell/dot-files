size="$(xrandr --query | awk '/*/ {print $1}')"

if [[ -z "$1" ]]; then
  echo "no file name provided!"
  exit 1
fi

if [[ -z "" ]]; then
  brate=2000k
fi


ffmpeg -f x11grab -video_size "${size}" -i $DISPLAY -f alsa -i default -c:v libx264 -c:a libmp3lame -b:v ${brate} "$1.mp4"
